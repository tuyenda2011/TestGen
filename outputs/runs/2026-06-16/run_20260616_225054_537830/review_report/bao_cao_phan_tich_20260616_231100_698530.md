<!-- framework: Playwright | run_id: run_20260616_225054_537830 -->
---
**Framework:** Playwright  |  **Run ID:** run_20260616_225054_537830
---

## Tóm tắt kỹ thuật
- **Framework:** Playwright (Python)  
- **Kỹ thuật:** Hybrid  
- **Kết quả chạy:** 5 passed, 0 failed (không có coverage artifact)  
- **Vấn đề chính:** Các kiểm thử có **kỳ vọng sai** so với yêu cầu, thiếu kiểm thử cho các trường hợp lỗi, và **đường dẫn nguồn sai** (`source_under_test.html` không tồn tại trong source được cung cấp).  
- **Tài liệu kiểm thử:** Test Plan JSON (TC‑001 … TC‑010)  
- **Mã nguồn được kiểm tra:** `ui_checkout_fixture.html`

## Lỗi nghiêm trọng
- **Bằng chứng:**  
  - Test `test_calculate_total_vip_tier_no_cap` kiểm tra `subtotal=200, weight=10, destination='domestic', tier='vip', fragile=false, credit=0` và **assert** `total == '191.00'` (dòng `assert total == '191.00'`).  
  - Yêu cầu (TC‑001) dự kiến **total='165.00'** (discount 24, shipping 15 → 200‑24+15 = 191 → nhưng mô tả lại là 165).  
  - Test `test_calculate_total_loyal_tier_weight_over_20_fragile` **assert** `total == '177.00'` trong khi TC‑003 dự kiến **total='197.00'**.  
- **Ảnh hưởng:** Kỳ vọng không khớp với yêu cầu khiến kiểm thử không kiểm chứng đúng logic kinh doanh; nếu logic thay đổi, các test sẽ vẫn “pass” mà không phát hiện lỗi.  
- **Cách sửa:** Cập nhật các `assert` sao cho khớp với **kỳ vọng thực tế** dựa trên công thức trong source (ví dụ: TC‑001 nên `assert total == '191.00'` hoặc chỉnh lại yêu cầu; TC‑003 nên `assert total == '177.00'` nếu công thức đúng). Đảm bảo mọi test phản ánh đúng **business_rules**.

- **Bằng chứng:**  
  - Đường dẫn file nguồn trong test: `page.goto((Path.cwd() / 'source_under_test.html').resolve().as_uri())` (dòng 15).  
  - Source thực tế có tên `ui_checkout_fixture.html`.  
- **Ảnh hưởng:** Khi chạy trong môi trường sạch, Playwright sẽ không tìm thấy file, gây lỗi “file not found”. Việc test hiện đang “pass” chỉ vì file `source_under_test.html` tồn tại ngẫu nhiên trong thư mục tạm, không phải là artefact đúng.  
- **Cách sửa:** Thay `source_under_test.html` bằng `ui_checkout_fixture.html` hoặc sử dụng biến môi trường để chỉ định đúng đường dẫn.

## Test còn thiếu
- **Bằng chứng:**  
  - Test Plan có các kịch bản TC‑006, TC‑007, TC‑008 (negative) và TC‑009 (integration) nhưng **không có test tương ứng** trong mã sinh.  
  - Không có kiểm thử xác nhận nội dung của `#error` khi hàm `calculateDiscount` hoặc `shippingFee` ném lỗi.  
- **Ảnh hưởng:** Không kiểm chứng được **validations** (subtotal >=0, weight >0, tier hợp lệ) và **xử lý lỗi** trong UI, vi phạm yêu cầu bảo mật/độ tin cậy.  
- **Cách sửa:** Thêm các test:
  1. Nhập `subtotal` âm → click → kiểm tra `#error` chứa thông báo `"subtotal must not be negative"`.
  2. Nhập `weight` ≤ 0 → kiểm tra thông báo `"weight must be greater than zero"`.
  3. Nhập tier không hợp lệ (ví dụ `gold`) → kiểm tra thông báo `"unsupported customer tier"`.
  4. Kiểm tra rằng khi lỗi xảy ra, `#total` không thay đổi (hoặc vẫn là giá trị trước).

## Assertion yếu
- **Bằng chứng:**  
  - Các test chỉ **assert** giá trị `total` và không kiểm tra `#error` trong các trường hợp lỗi.  
  - Không có kiểm tra trạng thái của các trường nhập (ví dụ: `checked`/`unchecked`) sau khi submit.  
- **Ảnh hưởng:** Các lỗi tiềm ẩn trong logic hiển thị lỗi hoặc reset form sẽ không được phát hiện.  
- **Cách sửa:** Thêm assertions:
  - `assert page.text_content('#error') == ''` sau các test thành công để chắc chắn không có lỗi ẩn.
  - Trong test lỗi, `assert page.text_content('#error')` chứa thông báo mong muốn và `assert page.text_content('#total') == ''` (hoặc giá trị cũ).

## Rủi ro maintainability
- **Bằng chứng:**  
  - Các test hard‑code đường dẫn file (`source_under_test.html`) và các selector ID.  
  - Không có cấu hình chung cho URL hoặc timeout, gây khó mở rộng khi UI thay đổi ID.  
- **Ảnh hưởng:** Khi UI thay đổi (ví dụ đổi ID), phải sửa mọi test thủ công.  
- **Cách sửa:** Định nghĩa **constants** cho URL và selectors trong một module chung, sử dụng `data-testid` (đã có) thay cho ID khi có thể.

## Gợi ý sửa ngay
1. **Sửa đường dẫn file nguồn** trong fixture `page.goto` thành `ui_checkout_fixture.html`.  
2. **Cập nhật các assert** để phản ánh đúng kỳ vọng của business rules (đối chiếu với TC‑001, TC‑003).  
3. **Thêm 4 test** cho các kịch bản negative (TC‑006, TC‑007, TC‑008, TC‑009) và kiểm tra nội dung `#error`.  
4. **Bổ sung assertion** kiểm tra `#error` rỗng trong các test thành công và kiểm tra `#total` không thay đổi khi có lỗi.  
5. **Tách hằng số selector** vào file `selectors.py` để giảm việc sửa mã khi UI thay đổi.  

---  

*Lưu ý:* Các đề xuất trên dựa trên bằng chứng thực tế từ source code, test plan và mã kiểm thử đã sinh. Việc thực hiện sẽ nâng cao độ tin cậy, độ bao phủ và khả năng bảo trì của bộ kiểm thử.