<!-- framework: Selenium | run_id: run_20260617_020617_758433 -->
---
**Framework:** Selenium  |  **Run ID:** run_20260617_020617_758433
---

## Tóm tắt kỹ thuật
- **Framework:** Selenium (Python)  
- **Kết quả chạy:** Tất cả 6 test hiện có đều **pass** (see execution output “6 passed”).  
- **Coverage:** Không có báo cáo coverage (`coverage_supported: False`).  
- **Vấn đề lớn nhất:** Các test không khớp với tên file HTML thực tế và thiếu các test cho các kịch bản **TC‑007** và **TC‑008** (thiếu kiểm tra lỗi destination không hợp lệ và credit âm).  

---

## Lỗi nghiêm trọng
- **Bằng chứng:**  
  - `load_page(driver)` trong mã test:  
    ```python
    uri = (Path.cwd() / "source_under_test.html").resolve().as_uri()
    ```  
    Tuy nhiên file nguồn thực tế là **ui_checkout_fixture.html** (xem phần `[SOURCE CODE]`).  
  - Test vẫn “pass” vì Selenium mở một file không tồn tại (trả về 404) hoặc mở một file HTML rỗng, nhưng **không kiểm tra** nội dung thực tế của UI.  
- **Ảnh hưởng:** Các test đang chạy trên một trang không phải là trang cần kiểm thử, do đó không thể xác nhận đúng hành vi của hàm `calculateDiscount`, `shippingFee` và xử lý form. Điều này làm mất tính đúng đắn của toàn bộ bộ test.  
- **Cách sửa:**  
  ```python
  def load_page(driver):
      """Mở file HTML fixture trong trình duyệt."""
      uri = (Path.cwd() / "ui_checkout_fixture.html").resolve().as_uri()
      driver.get(uri)
  ```

- **Bằng chứng:**  
  - Các kịch bản **TC‑007** và **TC‑008** trong Test Plan không có trong mã test.  
- **Ảnh hưởng:** Thiếu kiểm tra các trường hợp **negative / clarification** quan trọng (validation lỗi destination, credit âm), vi phạm yêu cầu “total calculation on form submission” và “validations”.  
- **Cách sửa:** Thêm hai test tương ứng, ví dụ:

  ```python
  def test_invalid_destination_shows_error(driver):
      load_page(driver)
      fill_form(driver, subtotal=50, weight=2, destination="moon",
                tier="standard", fragile=False, credit=0)
      submit_form(driver)
      assert get_error_text(driver) != ""
      # Kiểm tra rằng total không thay đổi hoặc bằng "0.00"
      assert get_total_text(driver) == "0.00"
  
  def test_negative_credit_handling(driver):
      load_page(driver)
      fill_form(driver, subtotal=100, weight=2, destination="domestic",
                tier="standard", fragile=False, credit=-5)
      submit_form(driver)
      # Giả sử ứng dụng không cho credit âm, phải hiển thị lỗi
      assert get_error_text(driver) != ""
  ```

---

## Test còn thiếu
- **TC‑007** – Kiểm tra thông báo lỗi khi `destination` không hợp lệ.  
- **TC‑008** – Kiểm tra hành vi khi `credit` âm (có thể là lỗi validation hoặc chấp nhận).  
- **Kiểm tra UI error element** cho các trường hợp negative (TC‑004, TC‑005, TC‑006) – hiện chỉ kiểm tra `JavascriptException` mà không xác nhận rằng phần tử `#error` được cập nhật.  

---

## Assertion yếu
- Các test **TC‑004, TC‑005, TC‑006** chỉ dùng `execute_script` và kiểm tra thông báo trong `JavascriptException`. Không có **assert** để xác nhận rằng UI (phần tử `#error`) hiển thị thông báo lỗi hoặc rằng form không cập nhật `#total`.  
- Các test **TC‑001‑TC‑003** chỉ kiểm tra `get_error_text == ""` và `get_total_text` – đủ, nhưng không kiểm tra rằng các giá trị đầu vào đã thực sự được đưa vào DOM (ví dụ: kiểm tra giá trị `value` của các input).  

---

## Rủi ro maintainability
- **Hard‑coded file name** (`source_under_test.html`) gây lỗi khi di chuyển hoặc đổi tên fixture.  
- **Edge driver** được khởi tạo mà không kiểm tra sự tồn tại của driver binary; trên môi trường CI có thể gây lỗi “driver not found”.  
- **Không có teardown** cho việc xóa cache pytest (`.pytest_cache` lỗi quyền) – có thể dẫn tới cảnh báo và ảnh hưởng tới các lần chạy tiếp theo.  

---

## Gợi ý sửa ngay
1. **Sửa đường dẫn file HTML** trong `load_page` để trỏ đúng tới `ui_checkout_fixture.html`.  
2. **Thêm test cho TC‑007 và TC‑008** theo mẫu ở trên, đồng thời kiểm tra phần tử `#error`.  
3. **Mở rộng các test negative** để xác nhận UI error hiển thị, không chỉ dựa vào `JavascriptException`.  
4. **Cập nhật fixture driver**: nếu có thể, chuyển sang Chrome/Chromium driver phổ biến hơn, hoặc cấu hình `EdgeService(executable_path=...)` để tránh lỗi môi trường.  
5. **Xóa hoặc cấu hình lại cache** để tránh warning `Access is denied` (có thể thiết lập biến môi trường `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` hoặc chỉ định thư mục cache có quyền ghi).  

---