<!-- framework: JUnit | run_id: run_20260619_100020_367773 -->
---
**Framework:** JUnit  |  **Run ID:** run_20260619_100020_367773
---

## Tóm tắt kỹ thuật
- **Framework:** JUnit 5 (JUnit Jupiter)  
- **Kết quả chạy:** Tất cả 21 test **pass**, không lỗi biên dịch, không lỗi runtime.  
- **Coverage:** 100 % (Jacoco) – đạt ngưỡng chất lượng *coverage*.  
- **Issue type:** none (không có lỗi thực thi).  
- **Blocker quan trọng nhất:** Không có lỗi ngăn chạy test.  
- **Artifact được xem xét:** `BankAccountTest.java` (test) và `BankAccount.java` (source).  

---

## Lỗi nghiêm trọng
Không phát hiện.

---

## Test còn thiếu
- Các test **out‑of‑scope** được liệt kê trong Test Plan (TC‑005 ~ TC‑012) không được hiện thực trong mã test vì chúng liên quan tới tính năng JUnit chứ không phải API `BankAccount`.  
- **Không có test** cho trường hợp **`transfer`** khi tài khoản nguồn không active (điều kiện `checkActive()` trong `transfer`). Đây là một trường hợp tiêu cực hợp lý dựa trên quy tắc `checkActive()` được gọi ở đầu phương thức `transfer`.  

**Bằng chứng:**  
- Test hiện có cho `transfer` chỉ bao phủ `null target`, `target inactive`, và thành công (TC‑017 ~ TC‑019).  
- Không có test nào mô phỏng `sourceAccount.deactivate(); sourceAccount.transfer(...);`.

**Ảnh hưởng:** Thiếu kiểm thử cho một luồng lỗi tiềm ẩn, có thể dẫn tới lỗi không phát hiện khi tài khoản nguồn bị vô hiệu hoá.

**Cách sửa:** Thêm test, ví dụ:

```java
@Test
void transfer_throwsExceptionWhenSourceInactive() {
    BankAccount source = new BankAccount("SRC", 100.0);
    BankAccount target = new BankAccount("TGT", 0.0);
    source.deactivate();
    IllegalStateException ex = assertThrows(IllegalStateException.class,
        () -> source.transfer(target, 50.0));
    assertEquals("Account is inactive", ex.getMessage());
}
```

---

## Assertion yếu
Không phát hiện.

Tất cả các test đều sử dụng `assertEquals`, `assertTrue`, `assertFalse`, hoặc `assertThrows` với kiểm tra thông điệp lỗi, không có assertion luôn đúng hoặc chỉ in ra log.

---

## Rủi ro maintainability
- **Số lượng test lớn (21)** có thể gây khó khăn trong việc duy trì khi yêu cầu nghiệp vụ thay đổi, nhưng không có dấu hiệu của **flaky** vì không phụ thuộc vào thời gian, mạng, hay hệ thống file ngoài `@TempDir` (không được sử dụng).  
- Các test được đặt tên theo chuẩn `TC_###_...` và `COV_retry_2_...`; việc trộn hai kiểu đặt tên có thể gây nhầm lẫn khi mở rộng.  

**Bằng chứng:**  
- Các test `COV_retry_2_*` được sinh tự động, trong khi các test gốc `TC_###_*` được viết theo kế hoạch.  

**Ảnh hưởng:** Khi thêm hoặc sửa đổi test, có thể xảy ra lỗi đặt tên không đồng nhất, gây khó khăn trong việc lọc/report.

**Cách giảm rủi ro:** Đưa ra quy ước đặt tên thống nhất (ví dụ: `TC_###_description`) và loại bỏ tiền tố `COV_retry_2_` nếu không cần thiết.

---

## Gợi ý sửa ngay
1. **Thêm test cho `transfer` khi tài khoản nguồn không active** (xem mục “Test còn thiếu”).  
2. **Chuẩn hoá tên test**: loại bỏ hoặc thống nhất tiền tố `COV_retry_2_` để tên test ngắn gọn, dễ đọc.  
3. **Bổ sung chú thích** cho các test tự động sinh (`COV_retry_2_*`) để người đọc hiểu mục đích và nguồn gốc.  

Nếu không muốn mở rộng hiện tại, các đề xuất trên có thể được thực hiện trong vòng sprint tiếp theo mà không ảnh hưởng tới kết quả hiện tại.