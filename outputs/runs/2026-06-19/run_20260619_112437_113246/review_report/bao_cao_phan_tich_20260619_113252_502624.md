<!-- framework: JUnit | run_id: run_20260619_112437_113246 -->
---
**Framework:** JUnit  |  **Run ID:** run_20260619_112437_113246
---

## Tóm tắt kỹ thuật
- **Framework:** JUnit (JUnit 5)  
- **Kết quả chạy:** Pass 100 % (17/17 test passed)  
- **Coverage:** 100 % (Jacoco) – đạt quality gate “coverage”.  
- **Vấn đề lớn nhất:** Không phát hiện lỗi nghiêm trọng nào ngăn việc chạy test.  
- **Artifact được xem xét:** `BankAccountTest.java` (test) và `BankAccount.java` (source).  

## Lỗi nghiêm trọng
Không phát hiện.

## Test còn thiếu
- **[out of scope]** Kiểm thử việc tạo tài khoản không hoạt động (`isActive = false`) thông qua một constructor hoặc builder cho phép thiết lập trạng thái này, vì trong source hiện tại không có constructor nhận tham số `isActive`.  
- **[out of scope]** Kiểm thử các quy tắc định dạng `accountNumber` (ví dụ: độ dài, ký tự cho phép) vì yêu cầu này không được mô tả trong source.  

## Assertion yếu
Không phát hiện.

## Rủi ro maintainability
- Các test phụ thuộc vào trạng thái nội bộ `isActive` thông qua phương thức `deactivate()`. Nếu logic `deactivate()` thay đổi (ví dụ: cho phép deactivate khi còn dư tiền), một số test (TC‑015, TC‑017) sẽ trở nên lỗi mà không nhận ra thay đổi trong yêu cầu.  
- Không có mock hoặc stub nào, nhưng các test thực hiện các thao tác thực tế trên đối tượng `BankAccount`; nếu trong tương lai lớp này thêm phụ thuộc vào hệ thống bên ngoài (ví dụ: service logging), các test hiện tại sẽ trở nên “flaky”.  

## Gợi ý sửa ngay
1. **Thêm tài liệu/ghi chú** trong test để giải thích vì sao `deactivate()` được dùng để tạo tài khoản không hoạt động, tránh nhầm lẫn khi thay đổi logic.  
2. **Bảo vệ các test khỏi thay đổi hành vi `deactivate()`** bằng cách không dựa vào việc tạo tài khoản không hoạt động; thay vào đó, nếu cần, có thể mở rộng source để cho phép constructor nhận tham số `isActive` và viết test riêng cho trường hợp này.  
3. **Kiểm tra thông điệp ngoại lệ** cho các trường hợp `deposit`, `withdraw`, `transfer` đã được thực hiện; nếu muốn tăng độ chắc chắn, có thể thêm `assertEquals` cho thông điệp trong TC‑008, TC‑011 (hiện chưa kiểm tra thông điệp).  

*Các đề xuất trên đều có thể thực hiện ngay mà không ảnh hưởng tới tính ổn định hiện tại của bộ test.*