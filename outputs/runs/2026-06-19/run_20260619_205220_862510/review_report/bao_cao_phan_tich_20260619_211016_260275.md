<!-- framework: Jest | run_id: run_20260619_205220_862510 -->
---
**Framework:** Jest  |  **Run ID:** run_20260619_205220_862510
---

## Tóm tắt kỹ thuật
- **Framework:** Jest  
- **Kết quả chạy:** Tất cả 16 test được sinh ra đều **PASS**, coverage 93.33 % (đạt quality gate).  
- **Artifact kiểm thử:** `generated.test.js` (import `source_under_test.js`).  
- **Artifact nguồn:** `orderPricing.js`.  
- **Vấn đề blocker chính:** Không có lỗi ngăn chạy, nhưng **các test TC-003 và TC-004** (kiểm tra `roundMoney`) bị thiếu vì không được ràng buộc với bất kỳ symbol nào trong source, dẫn đến lỗ hổng kiểm thử cho hàm `roundMoney`.  

---

## Lỗi nghiêm trọng
- Không phát hiện.

---

## Test còn thiếu
- **TC-003 & TC-004** – Kiểm tra `roundMoney` (round down / round up) không có trong bộ test sinh ra, mặc dù hàm này tồn tại trong source và được liệt kê trong `source_symbols`.  
  - **Bằng chứng:** `scope_validation.ambiguous_cases` chứa `["TC-003","TC-004"]` và log thực thi không có test nào tương ứng.  
  - **Ảnh hưởng:** Không có xác nhận rằng hàm `roundMoney` hoạt động đúng với các trường hợp làm tròn lên và xuống, có thể gây lỗi khi các giá trị tiền tệ không được làm tròn chính xác.  
  - **Cách sửa:** Thêm hai test cho `roundMoney` dựa trên dữ liệu trong test plan:

    ```javascript
    test('TC-003 roundMoney rounds down correctly', () => {
      expect(target.roundMoney(10.123)).toBe(10.12);
    });

    test('TC-004 roundMoney rounds up correctly', () => {
      expect(target.roundMoney(10.126)).toBe(10.13);
    });
    ```

- **Kiểm thử âm cho `shippingFee` (weight <= 0)** – Không có test cho trường hợp `weightKg` âm hoặc bằng 0, trong khi source ném lỗi `"weight must be greater than zero"`.  
  - **Bằng chứng:** `shippingFee` có đoạn `if (weightKg <= 0) { throw new Error("weight must be greater than zero"); }` nhưng trong test không có case tương ứng.  
  - **Ảnh hưởng:** Rủi ro lỗi không được phát hiện khi đầu vào không hợp lệ.  
  - **Cách sửa:** Thêm test:

    ```javascript
    test('shippingFee throws error for non‑positive weight', () => {
      expect(() => target.shippingFee(0, 'domestic')).toThrowError('weight must be greater than zero');
      expect(() => target.shippingFee(-3, 'domestic')).toThrowError('weight must be greater than zero');
    });
    ```

- **Kiểm thử cho destination không hỗ trợ** – Không có test cho `shippingFee` khi `destination` không phải “domestic” hoặc “international”.  
  - **Bằng chứng:** Source có `if (!["domestic","international"].includes(normalizedDestination)) { throw new Error(...); }`.  
  - **Ảnh hưởng:** Không bảo đảm hàm xử lý đúng các giá trị không hợp lệ.  
  - **Cách sửa:** Thêm test:

    ```javascript
    test('shippingFee throws error for unsupported destination', () => {
      expect(() => target.shippingFee(2, 'mars')).toThrowError('unsupported destination: mars');
    });
    ```

---

## Assertion yếu
- Không phát hiện.

---

## Rủi ro maintainability
- Không phát hiện.

---

## Gợi ý sửa ngay
1. **Thêm test cho `roundMoney`** (TC-003, TC-004) để lấp đầy lỗ hổng hiện tại.  
2. **Thêm test âm cho `shippingFee`** (trọng lượng ≤ 0) và **test cho destination không hỗ trợ** để tăng độ bao phủ các luồng lỗi.  
3. Kiểm tra lại **các test đã tồn tại** để đảm bảo mô tả trong test plan (ví dụ TC-012) khớp với công thức tính trong source; hiện tại chúng khớp, nhưng nên ghi chú lại trong comment để tránh nhầm lẫn trong các phiên bản sau.  

Áp dụng các sửa đổi trên sẽ hoàn thiện bộ kiểm thử, loại bỏ các khoảng trống còn lại và nâng cao độ tin cậy của module `orderPricing`.