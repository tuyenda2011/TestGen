<!-- framework: Postman script | run_id: run_20260620_204915_631144 -->
---
**Framework:** Postman script  |  **Run ID:** run_20260620_204915_631144
---

## Tóm tắt kỹ thuật
- **Framework:** Postman (Newman) script, sử dụng `pm.test` / `pm.expect`.  
- **Kết quả chạy:** `passed: True`, không có lỗi thực thi, coverage không hỗ trợ nhưng **quality_gate** đạt (`e2e_flow_assertions`).  
- **Phạm vi kiểm thử:** Các test case trong Requirement (TC‑001, TC‑002, TC‑004, TC‑005) đều có trong collection và đã chạy thành công.  
- **Trình độ blocker:** Không có lỗi nghiêm trọng ngăn chạy test; không phát hiện lỗi import, cú pháp, hoặc API tự tạo.  

---

## Lỗi nghiêm trọng
Không phát hiện.

---

## Test còn thiếu
- Không có test cho **đánh giá discount và shipping diagnostics** khi **storeCredit** được cung cấp và hợp lệ (ví dụ: `storeCredit: 10` trong TC‑001).  
- Không có test cho **trường hợp subtotal bằng 0** (được mô tả trong source nhưng không nằm trong Requirement).  
- Không có test cho **định dạng lỗi chi tiết** (ví dụ: kiểm tra trường `error.code` nếu có).  

*(Các test trên không bắt buộc theo Requirement, nhưng nếu muốn bao phủ đầy đủ các quy tắc kinh doanh, nên thêm.)*

---

## Assertion yếu
Không phát hiện.

---

## Rủi ro maintainability
- Các giá trị kỳ vọng (`total`, `discount`, `shipping`, `totalBeforeCredit`) được hard‑code trong script (ví dụ: `pm.expect(body.total).to.eql(100.5);`). Nếu quy tắc tính toán thay đổi, cần cập nhật thủ công nhiều test, gây rủi ro lỗi đồng bộ.  
- Không có kiểm tra schema JSON cho phản hồi thành công, nên nếu cấu trúc phản hồi thay đổi (thêm/đổi tên trường) các test hiện tại sẽ không phát hiện sớm.

---

## Gợi ý sửa ngay
1. **Tách dữ liệu kỳ vọng ra biến hoặc fixture**:  
   ```javascript
   const expected = {
       total: 100.5,
       diagnostics: { discount: 12, shipping: 22.5, totalBeforeCredit: 110.5 }
   };
   pm.expect(body.total).to.eql(expected.total);
   pm.expect(body.diagnostics).to.deep.eql(expected.diagnostics);
   ```
   Giúp cập nhật nhanh khi quy tắc thay đổi.

2. **Thêm kiểm tra schema JSON** cho phản hồi thành công (sử dụng `pm.response.to.have.jsonSchema(schema)`), giảm rủi ro thay đổi cấu trúc.

3. **Bổ sung test case** cho:
   - `storeCredit` hợp lệ (đảm bảo tổng tính sau credit đúng).  
   - `subtotal = 0` (đã có trong source nhưng chưa được yêu cầu, có thể đưa vào coverage).  
   - Kiểm tra chi tiết thông báo lỗi (ví dụ: `pm.expect(body.error).to.eql("storeCredit must be non‑negative")`).

4. **Đặt tên biến môi trường** (`base_url`) trong file `.env` hoặc collection variables để dễ thay đổi môi trường mà không sửa mã test.  

---